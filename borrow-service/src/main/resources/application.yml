server:
  port: 8083

spring:
  application:
    name: borrow-service
  datasource:
    url: jdbc:mysql://localhost:3306/lms
    username: root
    password: qwer1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
